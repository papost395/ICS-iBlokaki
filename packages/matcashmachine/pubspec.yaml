name: matcashmachine
description: "Dart package for communicating with MAT MIRKA III / INFOCARINA i57 III fiscal ECR devices via TCP/IP using the MCP protocol."
version: 0.1.0
homepage: https://github.com/your-org/matcashmachine

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Pure Dart package — no Flutter dependency required.

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
